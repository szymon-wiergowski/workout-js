// # Classes

// ## 1. Definicja
// Zdefiniuj `Shape`, która w konstruktorze otrzymuję dwa parametry `x` i `y`.

class Shape {

}


// ## 2. Dziedziczenie
// Napisz klasę `Circle` dziedziczącą po `Shape`, która w konstruktorze otrzymuję trzy parametry `x` i `y` i `radius`. 
// Klasa ma wywoływać konstruktor rodzica, i trzymac `radius` jako pole.

class Circle {

}